# deploy.conf — WiFi and deployment configuration
# Copy to deploy.conf and customize before running prepare-deployment.sh

# WiFi credentials (used in autoinstall.yaml netplan generation)
WIFI_SSID="ATTj6pXatS"
WIFI_PASSWORD="j75b39=z?mpg"

# Monitoring
WEBHOOK_HOST="192.168.1.115"
WEBHOOK_PORT="8080"
