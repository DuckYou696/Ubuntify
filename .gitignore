# Build output
*.iso
*.qcow2

# SSH credentials - NEVER commit private keys
ssh-*/
ssh/config

# Deployment config (contains WiFi credentials)
deploy.conf

# macOS
.DS_Store

# Sisyphus internal
.sisyphus/

# Node runtime
node_modules/
macpro-monitor/server.log
macpro-monitor/server.pid
macpro-monitor/logs/

# Large binary files (ISO images, VM disks)
*.vdi
*.vmdk
*.img
*.raw
